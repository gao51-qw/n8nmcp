name: Tests

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  vitest:
    name: Vitest (i18n + docs head)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Cache vitest
        uses: actions/cache@v4
        with:
          path: |
            node_modules/.vitest
          key: ${{ runner.os }}-vitest-${{ hashFiles('vitest.config.ts', 'package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-vitest-

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        run: npm test

      - name: Run docs head i18n integrity tests
        run: |
          npx vitest run src/i18n/__tests__/docs-head.test.ts \
            --reporter=verbose \
            --reporter=junit \
            --outputFile.junit=reports/docs-head-junit.xml \
            --coverage \
            --coverage.reporter=text \
            --coverage.reporter=html \
            --coverage.reporter=lcov \
            --coverage.reportsDirectory=coverage/docs-head \
            --coverage.include='src/i18n/docs-dict.ts' \
            --coverage.include='src/i18n/locales/docs/**' \
            --coverage.include='src/lib/seo-jsonld.ts'

      - name: Upload docs-head test report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: docs-head-test-report
          path: |
            reports/docs-head-junit.xml
            coverage/docs-head
          if-no-files-found: warn
          retention-days: 14
